# Production has no endpoint or credential defaults. Deployment must inject all
# three values through its secret/configuration facility.
spring.datasource.url=${AUKCIJE_DB_URL}
spring.datasource.username=${AUKCIJE_DB_USER}
spring.datasource.password=${AUKCIJE_DB_PASSWORD}

spring.h2.console.enabled=false
