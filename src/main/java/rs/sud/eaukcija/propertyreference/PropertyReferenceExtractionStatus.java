package rs.sud.eaukcija.propertyreference;

/** Extraction state only; geospatial resolution has its own independent status model. */
public enum PropertyReferenceExtractionStatus {
    EXTRACTED,
    NO_STRUCTURED_REFERENCE,
    NEEDS_REVIEW
}
